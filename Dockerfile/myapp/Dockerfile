# Use a minimal base image, like Alpine Linux
FROM alpine:latest

# Run an update command as a build step (optional, but good practice)
RUN apk update

# Set the default command to run when the container starts
CMD ["echo", "Hello, World!"]
